{
  "name": "agency-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs",
    "db:push": "drizzle-kit push --dialect sqlite --schema ./schema.mjs --dbCredentials.url ./sqlite.db"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "pino-pretty": "^13.0.0",
    "drizzle-orm": "^0.39.0",
    "express": "^5.2.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10"
  }
}
